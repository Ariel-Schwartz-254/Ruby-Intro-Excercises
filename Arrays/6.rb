names = ['bob', 'joe', 'susan', 'margaret']
names['margaret'] = 'jody'

# We are trying to pass in a string as an index number which must be an integer
# We can modify using the index method: names[names.index('margaret')] = 'jody'